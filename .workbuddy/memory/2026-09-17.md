# 2026-09-17

- 帖子详情页底部操作栏(点赞/Save/Comment)统一:新增 `configureActionButton(_:systemName:title:tintColor:)` 统一配置,图标统一 24pt(`UIImage.SymbolConfiguration(pointSize: 24)`),字号统一 14pt;Save 加 `bookmark`、Comment 加 `bubble.right` 图标,点赞 heart 也改为 24。
- 该按钮组改为传统 UIButton 属性(不再用 `UIButton.Configuration`),规避 configuration 忽略 `setTitleColor` 的坑;图标-文字间距用 `imageEdgeInsets.right = 2` / `titleEdgeInsets.left = 2`。
- 删掉已无调用的死代码 `makeActionButton(systemName:title:)`(内部正是用的 `.plain()` configuration,留着容易被误用)。
- HomeFeedCell 与详情页对齐:点赞/Save 同样用 `configureActionButton`(图标 24pt、字号 14pt、heart/bookmark 图标),并删掉旧的 `configureTextButton`(12pt);把 likes→save 的水平间距从 14 收到 10,给右侧 172 宽的 "View breakdown" 留余量(375 宽屏在 "Saved" 状态下几乎贴边)。
- 注意:`likesButton.heightAnchor` 目前被改成了 24(saveButton 是 34),和 24pt 图标刚好等高,视觉上偏紧;如觉得挤可调回 34。
- 用户在 Xcode 里把 HomeFeedCell 的按钮图标/字号下调到 12pt(此前我设的 24/14),并反馈文字显示不全 —— 以用户 Xcode 内实测为准,不要擅自改回大号。
- 后续:用户要求两个按钮各加宽 6pt → `contentEdgeInsets.right` 由 4 提到 10(4pt 抵掉 title 偏移,额外 6pt 留白)。该 cell 的按钮**没有显式宽度约束,靠 intrinsicContentSize**,所以加宽只能改 contentEdgeInsets 或 image/titleEdgeInsets 的等价补值。
- 修复 HomeFeedCell 点赞/收藏文字被截断:根因是 `imageEdgeInsets` / `titleEdgeInsets` **不计入 UIButton 的 intrinsicContentSize**,标题被 titleEdgeInsets.left 往右推但宽度没补 → 尾部被裁。改法:`titleEdgeInsets.left = 4` + `contentEdgeInsets.right = 4`(把间距用 contentEdgeInsets 补进固有宽度)+ `setContentCompressionResistancePriority(.required, for: .horizontal)` + `titleLabel?.lineBreakMode = .byClipping`,并把 likesButton 高度从 24 改回 34 与 saveButton 等高。
- 构建验证突破:之前一直以为 xcodebuild 被沙箱拦截无法编译,实测可行 —— `xcodebuild -project Wexlo.xcodeproj -scheme Wexlo -destination 'generic/platform=iOS Simulator' -configuration Debug -derivedDataPath DerivedData build CODE_SIGNING_ALLOWED=NO`(把 DerivedData 指到工程内目录)约 18s **BUILD SUCCEEDED**。以后改完直接用这个命令真编译,不再只用 swiftc -parse。
