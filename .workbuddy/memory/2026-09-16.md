# 2026-09-16

- 修复 ReportViewController 滚动区域遮挡顶部返回按钮(导航)的问题:scrollView 顶部约束从 `view.safeAreaLayoutGuide.topAnchor` 改为 `backButton.bottomAnchor + 25`,同时把 contentView 内标题的 78pt 顶部留白归零,保持初始视觉位置不变。滚动内容现在会被裁剪在返回按钮下方,不再钻到按钮底下。
- 备注:本机沙箱会拦截 xcodebuild 写 DerivedData/~/.swiftpm,验证编译可退而用 `xcrun swiftc -parse` 做语法检查。
- EULA 弹窗点 Cancel 直接退出 App:在 AppRootRouter.showAuthorization 里给 EULAViewController(继承 WexloDialogViewController 的 onCancel 回调)加 `exit(0)`。EULA 仅在此处弹出。
- Explore 页 "Recommended looks" 列表封面修复:原来只用 `post.mediaAssetName`(仅内置图片帖有值),本地图片帖/视频帖都显示兜底图。现按 HomeFeedCell 同样逻辑取封面:本地图片用 `WexloLocalContentStore.mediaURL(for:)` 读文件,视频用 `WexloVideoMedia.loadFirstFrame` 异步取首帧(带 representedMediaKey 防复用串图)。同时把 `allRecommendations` 从 stored let 改为计算属性,新发布的帖子能出现在列表里。帖子封面加载模式:bundled image → UIImage(named:);local image → contentsOfFile;video → loadFirstFrame 异步 + 占位图。
- 点赞/收藏红色状态同步:详情页(OutfitDetailViewController)点赞后 heart 系统图标红色+数字红色、收藏后 Saved 文字红色;首页(HomeFeedCell)点赞数据改为实时(WexloLikeStore.state),HomeFeedItem 增加 isLiked 字段(store 的 homeFeedItem(for:) 是唯一构造点,加 currentLikeUserID 辅助,与详情页 likeUserID 逻辑一致:authenticated→userID、guest→"guest"、absent→"anonymous")。HomeViewController.viewWillAppear 已有刷新,返回首页自动更新。
- 踩坑(重要):OutfitDetailViewController 的 likeButton 用了 `UIButton.Configuration`(`.plain()`),此时 `setTitleColor` 会被忽略 —— 文字颜色必须通过 `configuration?.baseForegroundColor` 设置。图片颜色用 `UIImage(systemName:)?.withTintColor(_:)`(baked,renderingMode 默认 .alwaysOriginal)不受影响。
- 首页 Save 接上真实收藏存储:`HomeFeedItem` 新增 `isSaved`(与 `isLiked` 同批),`WexloLocalContentStore.homeFeedItem(for:)` 用 `WexloSavedOutfitStore.isSaved(postID:userID:)` 计算,用户身份辅助统一改名为 `currentActiveUserID`。`HomeViewController` 的 `saveFeedItem()` 换成 `toggleSave(for:)`(调用 `WexloSavedOutfitStore.setSaved`,写 UserDefaults 并按 userID 分桶),新增 `refreshFeedItems()` 供 `viewWillAppear` 与收藏后刷新复用;保存后提示改为 Saved/Removed 两种文案。首页 Save 按钮按 isSaved 显示 "Saved" 红色 / "Save" 灰色。
- 顺带修复:Explore 搜索页结果复用同一个 `HomeFeedCell`,其 onSave 原来只弹 toast 不落库(接上 isSaved 后会显示"已收藏"假状态),现同样接上 `WexloSavedOutfitStore` 并在收藏后重跑 `performSearch()` 刷新。
- 数据链路:详情页/首页/搜索页收藏都写 `wexlo.saved.outfits`,并 post `.wexloSavedPostDidChange` 通知,`ProfileViewController` 监听后自动刷新 Saved tab。
