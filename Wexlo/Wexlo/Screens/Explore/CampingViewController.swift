import UIKit

final class CampingViewController: SceneDetailViewController {
    init() { super.init(setting: "Camping") }
    required init?(coder: NSCoder) { fatalError("init(coder:) is unavailable") }
}
